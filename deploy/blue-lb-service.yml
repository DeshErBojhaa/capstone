---
apiVersion: v1
kind: Service
metadata:
    name: capstone-service
spec:
    selector:
        app: blue
    type: LoadBalancer
    ports:
        - protocol: TCP
          targetPort: 8000
          port: 80
